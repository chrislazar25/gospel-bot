{
  "name": "gospel-bot",
  "version": "2.0.0",
  "description": "Daily gospel message sender via Green API",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "trigger": "node app.js --trigger-now",
    "preview": "node app.js --preview-now"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.9"
  }
}